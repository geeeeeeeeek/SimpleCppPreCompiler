#include <string>
#include <vector>

class tokenlizer
{
	std::vector<std::string> tokenlize(std::string context)
	{
		std::vector<std::string> token_vec;
//		while (context.find_first_not_of(" \n\t")!=-1)
//		{
//			int str_literal_pos =context.find_first_not_of("\"");
//			int white_space_pos = context.find(" ");
//			//white space before and after
//			int uniseperator_pos=context.find("=+-*/?");
//			int biseperator_pos=context.find("<<");
//			int end_of_sentence_pos=context.find(";");
//
//			int 
//		}
	}
};